const search=document.querySelector('#method-search');const cards=[...document.querySelectorAll('[data-method-card]')];function render(){const q=(search?.value||'').trim().toLowerCase();let shown=0;for(const card of cards){const ok=!q||(card.dataset.search||'').includes(q);card.hidden=!ok;if(ok)shown++;}const empty=document.querySelector('#method-empty');if(empty)empty.hidden=shown!==0;}search?.addEventListener('input',render);